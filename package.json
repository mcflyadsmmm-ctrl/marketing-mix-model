{
  "name": "mcfly-analytics",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=@mcfly/mer-core && npm run build --workspace=@mcfly/mer-engine && npm run build --workspace=app",
    "test": "npm run test --workspace=@mcfly/mer-core && npm run test --workspace=@mcfly/mer-engine",
    "typecheck": "npm run typecheck --workspace=app",
    "seed": "npm run seed --workspace=app"
  }
}
