# Mcfly Analytics — Shopify app configuration
# Linked Partner client_id. Production App URL = Fly host until app.mcflyads.com cert is ready.

client_id = "689279258bf1030a72ffd4b6f6dda989"
name = "Mcfly Analytics"
application_url = "https://mcfly-analytics.fly.dev"
embedded = true

[build]
automatically_update_urls_on_dev = true
include_config_on_deploy = true

[access_scopes]
# Truth MVP: order totals for cash MER. Expand only when pipes need it.
scopes = "read_orders"

[webhooks]
api_version = "2025-10"

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/uninstalled"
  topics = ["app/uninstalled"]

  [[webhooks.subscriptions]]
  uri = "/webhooks/app/scopes_update"
  topics = ["app/scopes_update"]

  # Mandatory App Store compliance webhooks (can share one URI; we use dedicated routes)
  [[webhooks.subscriptions]]
  uri = "/webhooks/compliance"
  compliance_topics = ["customers/data_request", "customers/redact", "shop/redact"]

[auth]
redirect_urls = [
  "https://mcfly-analytics.fly.dev/auth/callback",
  "https://mcfly-analytics.fly.dev/auth/shopify/callback",
  "https://mcfly-analytics.fly.dev/api/auth/callback"
]

[app_preferences]
url = "https://mcflyads.com/support.html"
