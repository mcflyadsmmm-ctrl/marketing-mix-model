/**
 * Love-UX6 — one-line Total ROAS definition for Overview (not a Data Dictionary).
 * Composes PRODUCT_NOUN so listing/desk stay on the same formula voice.
 */
import { PRODUCT_NOUN } from "./product-labels";

/** Visible under Overview cash religion — averages, not channel truth. */
export const OVERVIEW_TOTAL_ROAS_DEFINITION = `${PRODUCT_NOUN.totalRoas} = ${PRODUCT_NOUN.salesBasisShort} ÷ spend you entered — averages, not channel truth`;
