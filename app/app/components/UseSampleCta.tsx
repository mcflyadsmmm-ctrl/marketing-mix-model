/**
 * Retired Admin Sample switch. Public Sample shop lives on mcflyads.com/demo.
 * Kept as a no-op export so old imports do not break builds.
 */
export function UseSampleCta(_props?: {
  returnTo?: string;
  label?: string;
  variant?: "primary" | "secondary" | "tertiary";
}) {
  return null;
}
