# Mcfly well tokens — operator desk (not marketing OS)

**When:** 2026-09-19 · Founder lock: do not steal Amp/Lifetimely *marketing* charcoal for the analytics desk.  
**SoT CSS:** `app/app/styles/mcfly-desk.css` `:root` `--mcfly-well-*`  
**Mirror:** `site/assets/demo-desk.css` `--dd-well-*` · `site/assets/mcfly/mcfly.css` `--well-*`

## Direction

- Outer shell and inner scoreboards are the **same language**: paper/sky canvas, **white cards**, navy ink.
- Steal Lifetimely *product* stills: white KPI tiles, black numerals, grey labels, pastel series, green/red delta **text**.
- Do **not** paint `--mcfly-well-bg` charcoal. Dark navy is listing/marketing collage only.
- One accent: sky `#0284c7` for links, chart series, left pips — **not** giant cyan numerals.
- Empty stays **—** via `--mcfly-well-empty` — never `0.00×`.

## Tokens (lock)

| Token | Value | Role |
| --- | --- | --- |
| `--mcfly-well-bg` | `#ffffff` | Well / card canvas |
| `--mcfly-well-bg-elev` | `#f7fbfe` | Elevated KPI tiles / sky tint |
| `--mcfly-well-ink` | `#0f1720` | Primary numerals and type |
| `--mcfly-well-mute` | `#5b6b7c` | Labels / hints |
| `--mcfly-well-line` | `rgba(15, 23, 32, 0.10)` | Hairlines |
| `--mcfly-well-accent` | `#0284c7` | Series / pip / focus (not hero value) |
| `--mcfly-well-accent-soft` | `rgba(2, 132, 199, 0.12)` | Wash / fill |
| `--mcfly-well-truth` | `#047857` | Positive deltas |
| `--mcfly-well-truth-soft` | `#ecfdf5` | Truth wash |
| `--mcfly-well-empty` | `#7a8b9c` | Em dash empties |
| `--mcfly-well-radius` | `14px` | Corners |
| `--mcfly-well-shadow` | `0 1px 2px rgba(15, 23, 32, 0.06)` | Soft cool depth |

`--dd-well-*` and `--well-*` must match these roles.

## Hero tiles

- Background: `--mcfly-well-bg-elev` or `--mcfly-tint-sky` (`#f0f7fc`). **No** gradient to `#101820`.
- Hero value color: `--mcfly-well-ink` (not accent cyan).
- Left pip / chart stroke may use accent.

## Forbidden in desk + live slices

`#0c1219` `#141c26` `#101820` `#161f2a` `#5ee7f0` as well fill or hero type.

## Religion

Zero spend/ROAS on Overview. No purple glow. No Amp mint clone. H1 / Install / $39 / SAMPLE lock unchanged.
