# Megaprompt — enterprise third pass (after v422)

Paste this into the Conductor. **The desk is not finished.** Two megaprompts already ran. The first cooked through v416. The second cooked v417–v422 and ranked 12 more ships. Rank 7 of that queue is in review. Ranks 8–12 are queued. Reciting that list, or v422, as “enterprise-ready” is the failure this pass exists to stop.

Marty: recently it has felt like agents think everything is perfect. It is not. A $1M–$10M Shopify operator will cancel $39 in week two if the open lane still lies, still hides the primary number on a phone, still paints SAMPLE as Live, or still cannot answer the next order-history question their Saturday operator already asks. **Find many significant improvements. If a scout returns fewer than ten new ones, it stopped too early. Send it back.**

---

## Tip (floor, not a finish)

| Item | State |
| --- | --- |
| Fly | **v422** · merge `b80d263` (#171 DeskLane open) · image `deployment-01M34MG7QX41A2DR1H13KAMN0V` · health ok 2026-09-22T13:23Z |
| Scoreboard stamp | `3608683` on `cursor/spend-trust-recurring` (docs only, no second Fly) |
| Live | **PARKED** |
| ShopifyQL / `read_reports` | **HOLD** — not a craft blocker |
| Smoke | waived |
| Next craft already named | Rank 7 quiet-then-back `$` · `#172` · `cursor/quiet-back-dollars-5bc6` · SHA `0af283e` · Reviewer grading · **no Fly until PASS** |
| After that (already ranked, not a find) | Rank 8 whale ticket / RFM flow · rank 9 Orders mix · rank 10 guest `$` + Other source names · rank 11 compete rows 2–3 + annual base · rank 12 compete 4/6/7/8 + launch-week class |
| Prior notes | `#148`–`#152` first enterprise wave · `#158` second megaprompt · `#159`–`#161` `#163`–`#165` second scout + queue. Floor, not a finish. Do not re-run those waves as the same five files. |

**In flight (do not rebuild, do not “find” as new):** this period’s Shopify Total Sales from identified buyers whose previous order was already past that shop’s wait; copyable first-time `$` via `growthStandupCopyText`; next wait after they came back; first→last + inter-order gap; 8-buyer seal; guests out. Conductor merges `#172` + one Fly only after Reviewer PASS.

**Model:** inherit the Conductor. Do **not** pin `grok-4.7-xhigh` (rejected). Do not pin Grok 4.6.

Granola is not authenticated in this environment. Do not stall for meeting notes.

---

## What “enterprise” means on this pass

A multi-million-dollar Shopify store keeps **$39/store/month** after the first week because:

1. The question is on the **open lane** of a painted tab (not a settings wall, not a sixth tab, not a badge).
2. The empty tells the **truth** (dash / not on file / still syncing — never a fake `$0`, never `0×`, never a SAMPLE catalog on Live).
3. The **phone** (Admin iframe ~390px, `max-width: 36rem`) can read the primary number. Six-figure Shopify Total Sales must fit. Snowdevil `$68k` does not prove this.
4. A **teammate** can copy one sentence that names Shopify Total Sales and the window, without a Meta login.
5. A **Saturday operator** at 2:55pm can see through-this-clock and the period they manage, not last year’s evening and not a 90-day board when they picked this month.

Enterprise is not a visual refresh, a longer Settings page, a badge that says “enterprise,” or “we already have LTV.” It is jobs a $5M store still cannot do on this desk. If you cannot name ten of those jobs with file evidence, you did not look.

---

## Locks (breaking these is REFUSE, not a find)

- Painted IA: Overview → Orders → Customers → Spend → Goals. Growth and LTV are Customers chips. Never a sixth tab.
- One plan: $39/store/month after a 7-day trial. No Free plan. No GMV / order ladder.
- Total ROAS = Shopify Total Sales ÷ entered spend, or **—**. Never `0×`. Never a fake `$0`.
- Product titles are not stored. SAMPLE must not invent a catalog Live cannot store.
- Guests out of returning. Missing last year is not on file, never `$0`. Codes are names, not percents (`WELCOME10` is not 10%).
- Refuse: COGS / P&L / gross profit as hero, pixels, MTA, “true ROAS,” sessions/visitors, Amazon, Recharge/Skio MRR, invented stars/installs/metrics, Live unpark, Partner Submit, outbound review sends, person name/email/city, industry benchmarks, example-store figures from competitor blogs.
- Do not Fly from `marketing-mix-model` on `cursor/clean-revamp-v8`. App URL stays `https://mcfly-analytics.fly.dev`.
- One writer per surface. Reviewer PASS before merge + **one** Fly. Fewer bigger PRs. A Fly requires a new merchant sentence.
- Do not execute `docs/plans/2026-09-22-shopifyql-wait-queue.md` or `#137` / `#145`. ShopifyQL stays HOLD.

HOLD remains HOLD until the fact is on the order or Marty takes the call: refund processing date, country code, subscription-checkout bit, gift-card product flag, automatic discount titles, product title/SKU column, order rows past 24 months, product subtotal vs Total Sales, Whale → Admin door, million-order crawl as shipped, POS drawer tape, VAT-out, Stripe/PayPal time zones, ShopifyQL.

---

## Already on the tip — recycling is a scout FAIL

Do not list these as discoveries. Do not recook them. File-level depth on a leftover is allowed. Restating the merchant sentence is not.

**Shipped Fly notes**

- v408 starter / open-lane LTV
- v409 promo LTV, source LTV (web / POS / Shop), spend paste, empty spend as —, deleted day as `$0`, unique SAMPLE buyers on the paste book
- v410 morning sentence
- v411 period-total hero + phone year cards
- v412 live catalog empty tells the truth
- v413 returning mix from the stored book
- v414 Orders intelligence on the picked period, code dollars, returns climbing, demo mounts the same board
- v415 same-clock compare (364-day weekday shift, shop-local clock, custom from/to = calendar dates last year, Feb 29 null, missing prior day null, clock sentence once on OverviewFirstViewport)
- v416 third-order ticket / wait / reach on Customers → Growth (`buildOrderSteps`, 8-buyer seal, guests out, 1st wait —, 4th+ wait 3rd→4th only)
- v417 Goals honesty: SAMPLE does not confirm margin / invent 0.35 BE / `$800k` stretch; demo Goals mounts Admin stack; “Kept after margin.” deleted. SpendMix “Covering break-even.” only when `breakEvenMer != null`
- v418 phone Goals: `ThisMonthPlanStack` Goal/Actual/Prior on first fold; 36rem table wrap; Orders/Customers/Growth peeks wrap; panel chips 0.88rem; CPA keeps `rangeLabel`
- v419 Spend pair: `explorerMer` null when sales not on file; `spendPairCoverage.withholdRatio`; `CopySpendPair`; `formatOnlineRoasLine` uses `totalSales * mix.online`; Live week uniques from paste index; demo payback Snowdevil LTV + `historyLimited={false}`; day-0 `earned: false`
- v420 book coverage: unpaid till = 90 closed days; paid = up to 24 months; `orderBookDepth` **required** (no `paid_full` default); pending/authorized/COD/Klarna in Total Sales; this shop not company book; Spend first fold `spendFirstFoldSalesHint` for truncated today; YTD copyable
- v421 Goals year clock: empty `parseGoalInput` → null; `upsertYearSalesGoals` deletes a cleared month; typed `0` still saves; YTD/QTD skip a cleared month; `bookYear !== todayYear` withholds next-month-from-today; month-close uses selling days; quiet certified `$0` in so-far not in the rail; remaining spend paced on days that already have spend; stub unpaid month withheld; `$80k` ceiling example dropped; implied identified buyers = goal ÷ typical order at 8-order floor; Goals MTD discloses truncated today
- v422 DeskLane: later `defaultOpen` true opens; later false does not force-close; hash + `?panel=` open Depth / Add spend / Mix close; button + `aria-expanded` + `hidden={!open}`

**`OrderFact` already has** `unitCount`, `discountCode` (code only), `sourceName`, `grossAmount`, `amount`, `lifetimeOrders`. Day totals go back five years. Order rows do not.

**Copy already on the desk:** morning strip, `CopySpendPair`, `CopyYtdSales`, days-to-second on Growth, Goals habit sentence. Rank 7 adds first-time `$` copy — do not “find” that.

---

## Already ranked — not “new finds”

`docs/plans/2026-09-22-enterprise-next-queue.md` on `origin/cursor/next-cook-queue-5bc6` (`#165`). A scout that only restates this list **failed**. You may add file-level depth. Your **quota of new finds is on top of this list**.

| Rank | Sentence (already owned) | Branch |
| --- | --- | --- |
| 7 in flight | Quiet-then-back `$` + copyable first-time `$` | `cursor/quiet-back-dollars-5bc6` `#172` |
| 8 queued | Whale 1 ticket hidden; watchlist stops at 8 with no “N more”; cold share uncopied; RFM is a still photo | `cursor/whale-ticket-5bc6` |
| 9 queued | This month `$` by 1st/2nd/3rd/4th+; first-time vs returning ticket; tax/shipping slice; period concentration; new vs returning hour; discount `$` vs last September; kept share vs last year; first vs returning discount depth; dollars per unit; `$0` reship count | `cursor/orders-step-mix-5bc6` |
| 10 queued | Guest checkout as **dollars**; source names inside Other; returns climbing on POS vs online | `cursor/guest-source-returns-5bc6` |
| 11 queued | Compete rows 2–3 (buyer age; new `$` vs returning `$` vs same quarter last year); annual installed-base retention | `cursor/overview-buyer-age-5bc6` |
| 12 queued | Compete rows 4, 6, 7, 8; launch-week **class** (not Overview’s already-shipped launch-week compare); orders per month of life | LTV chip / `ltv-depth.ts` |

**Named leftovers from shipped cooks — deepen or skip, do not re-rank as the whole note:**

- v421: YTD **percent** can still look ahead because February sales stay in actual while February is omitted from the plan. `ThisMonthPlanStack` shows typed `$0` as — while the month input shows `"0"`. `demo.goals.tsx` still `goals: Array.from({ length: 12 }, () => 0)` but `hasGoal` is `goal > 0`.
- v422: Mix close chip also opens the weekday more-fold as a wrap/panel side effect.
- v420: Unlock banner is still Customers-only; Overview first fold already names the 90. Compact `PeriodControl` does not paint caption but still requires `orderBookDepth`. True `shopifyOrderWindowLimited` ~60-day banner stays. `site/pricing.html` “24 months” is a Marty Pages nit, not a desk cook.
- v419: payback still interpolates through a day-0 `$0` start (`cash-payback.ts` `earned: false`). Show the anchors or stop printing the day count was `#152` still-PASS. Refuse causal / ads-manager payback.
- `uninstall-friction.test.ts` still requires the string “24 months of orders” on the unlock banner (paid clause). Do not unpark Live. Do not change `LIVE_UNPAID_INGEST_DAYS = 90`.

HOLD still HOLD: Whale → Admin door, million-order crawl as shipped, refund `processed_at`, VAT-out, POS drawer tape.

---

## Surfaces the last two waves barely finished (hunt here first)

Last queues lived on Overview first fold, Orders intelligence, Customers mix / Growth steps, Goals year honesty, Spend pair, DeskLane. A scout that never opens **Settings, billing/trial copy, LTV leftover boards, Spend leftover after the pair, Goals leftover after the year clock, phone leftover after the Goals stack, Orders leftover that is not rank 9, or public `/demo` vs Admin `/app` on Spend/Settings** failed.

Walk all of these as the owner of a store doing **real volume** (hundreds of thousands this month, not Snowdevil toy dollars):

| Surface | Start files |
| --- | --- |
| Overview leftover | `OverviewMixForecast.tsx`, weekday fold after v422 wrap side effect, coverage line vs compact `PeriodControl`, ~60-day Shopify window banner |
| Orders leftover | anything **not** rank 9’s mix list: copyable Orders sentence, six-figure nowrap remaining, demo vs Admin remaining, POS vs online that is not rank 10 |
| Customers leftover | RFM/whales are rank 8 — skip as a find. Returning `$` vs returning **rate**. Unknown `lifetimeOrders` stuffed into 1st. Guest **percent** tile (dollars are rank 10). |
| LTV leftover | `CustomersLtvSection.tsx`, `LtvFirstProductDrivers.tsx`, `LtvPromoBoard.tsx`, `LtvProductBoard.tsx`, `LtvWhaleRecency.tsx`, `ltv-depth.ts` `cohortLtvCurves` (exists — is it painted?), 365 withheld vs history-limited |
| Spend leftover | `CpaPaybackDesk.tsx` anchors, `CpaExplorer.tsx`, paste unique Live weeks, POS-in-ads vs `formatOnlineRoasLine`, public `/demo/spend` “does not save” vs Admin `#mcfly-spend-add`, explorer six-figure nowrap |
| Goals leftover | YTD % look-ahead, typed `$0` vs —, implied spend ceiling remaining, forecast remaining, copyable year-plan sentence, phone leftover after `ThisMonthPlanStack` |
| Settings / billing | `app.settings.tsx`, `demo.settings.tsx`, shop timezone vs Overview clock, trial vs unpaid 90 vs paid 24, plan copy, uninstall |
| Phone leftover | whale 430px four-column is rank 8. Hunt LTV windows, Spend explorer, CPA table, Orders intelligence leftover, Goals leftover after the stack |
| SAMPLE vs Live leftover | remaining overlays after v417; `demo.*` vs `app.*` mount gaps that v414/v417 did not close |
| Copy leftover | Orders, LTV, Goals year, Spend leftover. Morning / Spend pair / YTD / days-to-second / rank 7 first-time `$` are taken |
| Site / listing | only if the public sentence disagrees with the desk. Do not invent reviews or installs. Pages deploy is Marty. |

---

## Hunt questions a lazy scout will skip

The 2026-09-22 second megaprompt asked 15 questions. Those produced ranks 1–12. **Do not answer them again as the note.** Answer **these**, with file evidence. “Looks fine” is not an answer.

1. After v421, does YTD **percent** still treat a cleared month as 0 plan while that month’s Shopify Total Sales sit in actual? Would a $5M operator change Saturday because of it?
2. Typed `$0` vs dash: which Goals cells still disagree with the month input? Is that a lie or a nit?
3. Which primary numbers on **LTV, Spend explorer, CPA, Settings** still wrap, truncate, or hide at 36rem now that Overview year cards and Goals month stack exist?
4. Where does SAMPLE still paint a number Live cannot store (catalog was v412; margin was v417; first-product titles copy exists — are there more overlays)?
5. Where does copy still say 24 months / “already on this desk” / trial=paid while unpaid ingest is 90, **other than** the Customers unlock banner and the Overview coverage line v420 already named?
6. Mix close also opens the weekday fold. What **other** chip / hash / `defaultOpen` pairs still open the wrong lane, or open two lanes, after v422?
7. Payback still interpolates through day-0 `$0`. Are the anchors painted? If not, is the day count still a lie a $5M operator will act on?
8. What can a second seat copy on **Orders, LTV, Goals year** that they still cannot? Name the sentence. Rank 7 owns Growth first-time `$`.
9. Quiet Saturday: `$0` today, last year not on file — which **Spend / Goals / LTV** boards still say “loading” or paint a finished zero?
10. `cohortLtvCurves` exists in `ltv-depth.ts`. Is it on the open LTV lane? If not, what merchant sentence is missing — and is it rank 12’s launch-week class or a different grain?
11. Unknown `lifetimeOrders`: where is it stuffed into first-time / 1st-step / new `$` instead of its own empty?
12. Online Total ROAS uses `totalSales * mix.online`. Where does POS spend still sit inside an ads ROAS the merchant will read as Meta?
13. Public `/demo/spend` and `/demo/settings` vs Admin: which boards, forms, or clocks mount on one and not the other **after** v417/v419/v420?
14. Shop timezone: Overview clock is shop-local. Which Spend / Goals / CPA windows still use host `Date` or UTC day keys?
15. `unitCount` / `grossAmount` / `discountAmount` — which enterprise question do they unlock that is **not** already rank 9 (dollars per unit, tax/shipping slice, discount `$` vs last September, `$0` reships)?
16. Six-figure month + thin last year on **Customers first fold / LTV windows / Spend mix**, not Overview glance: what nowrap or fake 0% still ships?
17. Billing / trial: in-app plan copy vs unpaid 90 vs `$39`. Does Settings or the upgrade bounce still describe a book the crawl does not keep?
18. Native Shopify Analytics 2026 (and Shopify Hourly / Sales by customer / ABC) still sells an order-history job Mcfly’s stored book can answer that is **not** ranks 8–12. Cite the 2026 page. Do not paste their example-store numbers.
19. Lifetimely / Peel / Triple Whale / TrueProfit / Putler / Repeat Customer Insights **2026** pages: one job each **beyond** compete rows 2–8 and ranks 8–12. Reciting “Customer Grids flow” (rank 8) or “purchased X times as `$`” (rank 9) is FAIL.
20. Public 2024–2026 operator threads **not** already in `#148` / `#161` (not tonytapay / Kayleigh / Wing-roro / petgrocer / MagnumFonseca / richjeff / bemo2 / Ohlala-equestrian / joshroban). New complaints. Reciting that cast is FAIL.
21. What breaks when this month is `$417,392` and last year is not on file — on a board **other than** Overview YoY and Goals year?
22. Company / B2B / draft / staff / Shop Cash: which of those strings are already on `sourceName` or order flags and currently dumped into Other (Other **names** are rank 10 — this is a different job only if the dollars or clock are wrong, not the label list)?
23. Truncated today: Overview and Goals MTD disclose it. Which Spend / Orders / Customers first-fold numbers still treat a partial today as a finished day?
24. What remaining SAMPLE vs Live disagreement would make a $5M operator who bought from `/demo` distrust the installed app **after** v417 mounted the Goals stack?

If you cannot fill ten rows that are **not** ranks 7–12, you failed. Nits do not count toward ten.

---

## Fleet (five read-only scouts, then one writer)

Scouts are **read-only**. Each owns **one new file**. No `app/` edits. No Fly. No shared file. No `gh pr create` (403). Push the branch; the Conductor opens the draft PR.

If a scout returns fewer than **ten significant new gaps** (not in “already on the tip,” not a restatement of ranks 7–12 without new evidence), the Conductor sends it back. Nits, comment fixes, CSS color, and “move the padding” are not gaps.

| Scout | Branch | File | Job |
| --- | --- | --- | --- |
| Desk truth | `cursor/third-desk-truth-5bc6` | `docs/ops/research/2026-09-22-THIRD_DESK_TRUTH.md` | Walk all five tabs + Growth/LTV chips + Settings on the **v422** tip (and `#172` if merged). Merchant sentences for jobs the stored book can already answer that no open lane asks. Skip ranks 7–12 as finds. |
| Craft holes | `cursor/third-craft-holes-5bc6` | `docs/ops/research/2026-09-22-THIRD_CRAFT_HOLES.md` | Lies, empties, SAMPLE vs Live leftover, banners leftover, payback anchors, YTD % look-ahead, typed `$0`, wrong-lane opens after v422, demo/admin mount gaps leftover. File + line evidence. |
| Compete | `cursor/third-compete-5bc6` | `docs/ops/research/2026-09-22-THIRD_COMPETE.md` | 2026 public pages. Jobs **beyond** compete rows 2–8 and ranks 8–12. Native Analytics included. Do not paste example-store numbers. |
| Operator | `cursor/third-operator-5bc6` | `docs/ops/research/2026-09-22-THIRD_OPERATOR.md` | Public Shopify community / Reddit / app-review complaints from operators of **real volume**, **new threads**. Reciting the previous operator casts is FAIL. |
| Volume desk | `cursor/third-volume-desk-5bc6` | `docs/ops/research/2026-09-22-THIRD_VOLUME.md` | Saturday operator of a $5M–$10M store: phone leftover, teammate copy leftover, quiet day leftover, six-figure month leftover, thin last year leftover, unpaid 90 vs paid 24 leftover. Snowdevil `$68k` does not count. |

Then **one synthesizer** on `cursor/third-cook-queue-5bc6` writes `docs/plans/2026-09-22-enterprise-third-queue.md`.

The synthesizer:

- Reads all five notes plus `#165` (second queue) plus this megaprompt.
- Collapses shared surfaces into one cook.
- Ranks **significant** ships only. Each ship is one merchant sentence a $5M store cannot say today.
- **Does not restart v408–v422 or `#172`.** Does not recook ranks 8–12 unless a new note proves the ranked sentence was wrong; then re-rank with evidence, do not duplicate.
- **May insert new ranks ahead of whale (old rank 8)** if a new hole is a bigger Saturday lie. Say so. Remaining old ranks 8–12 stay on the list unless absorbed.
- Top of this new list becomes the scoreboard “Next significant ship” only after `#172` PASS + Fly (or Marty skips it). Until then the scoreboard stays quiet-back.
- Nits, SoT stamps, and re-audits of a hole already on the tip do not make the list.
- Target **at least twelve ranked cooks** if the notes support it. If they do not, the synthesizer failed and must send scouts back — not shrink to three nits and call the desk done.

---

## How a scout works

1. `git fetch origin cursor/spend-trust-recurring` and branch from that tip (`3608683` or later). If `#172` has merged, use the new tip and do not list quiet-back as missing. Also fetch `origin/cursor/third-pass-megaprompt-5bc6` and **read this file**.
2. Read `docs/ops/SCOREBOARD.md`, this megaprompt, `docs/plans/2026-09-22-enterprise-next-queue.md` on `origin/cursor/next-cook-queue-5bc6`, and the 2026-09-22 `NEXT_*` notes as a **floor**.
3. Read the tip code. Walk Overview, Orders, Customers (Growth, LTV), Spend, Goals, Settings. Public `/demo` and Admin `/app`. 36rem and desktop. Six-figure fixtures, not Snowdevil `$68k`.
4. For every gap: merchant sentence, tab/chip, files with line evidence, why a $5M store cares, **why it is not a nit**, PASS / HOLD / REFUSE.
5. PASS = one cook can ship it without a new scope, a new tab, or a lie.
6. Open nothing. Push the one markdown file. Return: branch, SHA, **ten or more new sentences**, and the ones you refused.

Conventional commit: `docs: …`. Do not commit `.superpowers/`. Do not commit leftover `NEXT_CRAFT_HOLES.md` / `NEXT_VOLUME.md` untracked files.

---

## Conductor after the five notes exist

1. Open five draft PRs with ManagePullRequest (base `cursor/spend-trust-recurring`). Do not put cursor.com/agents URLs in PR bodies.
2. Spawn the synthesizer. Open its draft PR. **Do not merge research PRs** unless asked.
3. Do **not** start a new craft while `#172` is ungraded. One implementer at a time. Rank 8 of the **second** queue stays queued until PASS + Fly, unless the third-pass synthesizer has already re-ranked and `#172` has shipped.
4. After `#172` PASS + Fly, stamp the scoreboard with **rank 1 of the third-pass queue** (or old rank 8 if the synthesizer kept it first). Then one cook.
5. Do not fan four craft agents onto `mcfly-desk.css`, `customers-analytics.ts`, `DeskLane.tsx`, or Overview.

---

## Kill list

- Do not report the niche is owned. Do not write “solid,” “enterprise-ready,” “the desk is complete,” “only nits remain,” or “the last queue covered it.”
- Do not stop because v408–v422 exist. Do not stop because ranks 8–12 exist.
- Do not propose COGS, pixels, sessions, Amazon, Recharge, Slack product, or a sixth tab as the enterprise leap.
- Do not invent revenue, review counts, install counts, or Mcfly stars.
- Do not execute the ShopifyQL-wait queue.
- Do not unpark Live. Do not Partner Submit. Do not send review asks.
- Do not open a second cook beside `#172`.
- Do not Fly a nit.
- Do not change `LIVE_UNPAID_INGEST_DAYS = 90`. Do not edit `fly.toml` scopes.

---

## Significant vs nit (grade the scouts with this)

**Significant:** a $5M operator changes what they do on Saturday, or stops distrusting a number, or can copy a sentence their operator Slack needs. File evidence. One merchant sentence.

**Nit:** padding, eyebrow synonym, icon, comment, “add a tooltip,” restating a shipped rank, restating ranks 7–12 with no new file, a color pass, a sixth tab to look serious, Mix-close also opening weekday **unless** you prove a second wrong-lane class, `site/pricing.html` 24 months.

A Fly requires a new merchant sentence. Copy nits and SoT stamps do not deploy.
