# Shot 5 — Spend CSV listing asset (2026-07-29)

## Deliverable

| Field | Value |
| --- | --- |
| Path | `docs/listing-assets/shots/05-spend-csv.png` |
| Dimensions | **1600×900** RGB PNG |
| Source | **Mock** (Polaris-faithful PIL render) — not live Shopify Admin |
| Caption | Select platforms → export daily → combine |
| HOLD | `05-HOLD-marketing-site-do-not-upload.png` — **still do not upload** |

Raw twin: `docs/listing-assets/shots/raw/05-spend-csv-raw.png`  
Letterbox helper: `bash scripts/listing-shot-05.sh /path/to/crop.png`

## How produced

1. No Shopify Admin session / browser auth available in this agent lane.
2. No existing Spend Admin crop in `docs/listing-assets/` (only HOLD marketing site).
3. Built a desk-faithful Spend UI mock from `app.spend.tsx` + `mcfly-desk.css` patterns:
   - Step 1 platform checkboxes (Meta + Google checked; Other catch-all checked)
   - Step 2 blank template preview with **Day / Meta / Google / Other** columns
   - Step 3 combine native exports (per-platform CSV slots + Combine & import)
   - **● SAMPLE preview** badge; SAMPLE copy on fake amounts
4. Wrote 1600×900 directly, then ran `listing-shot-05.sh` (identity letterbox).

## Founder re-capture (preferred)

When Admin session is available:

1. Sample desk **ON** (or seed demo) → open `/app/spend?shot=1`
2. Ensure Meta + Google (+ Other if showing Pro columns) checked; expand combine if needed
3. Crop iframe only (~1600×900, no browser chrome)
4. `bash scripts/listing-shot-05.sh /path/to/crop.png`
5. Replace mock; update this note to **live Admin**

Religion: cash spend CSV desk only — never upload HOLD marketing-site shot as #5.
