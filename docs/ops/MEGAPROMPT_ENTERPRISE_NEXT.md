# Megaprompt — enterprise next pass (after v414)

Paste this into the Conductor. **The desk is not finished.** The last megaprompt was 47 lines and produced six ranks. Four of those ranks are on the tip. Rank 5 is in review. Rank 6 is queued. A still-PASS list sits uncooked. Goals and Spend were barely touched. Reciting v414 as “enterprise-ready” is the failure this pass exists to stop.

A $5M–$10M Shopify operator will cancel $39 in week two if the open lane still lies, still hides the primary number on a phone, still paints SAMPLE as Live, or still cannot answer the next order-history question their Saturday operator already asks.

**Do not congratulate the tip. Find many significant improvements. If you find fewer than eight new ones, you stopped too early.**

---

## Tip (floor, not a finish)

| Item | State |
| --- | --- |
| Fly | **v414** · merge `12c9950` · image `deployment-01M345SQV30P8SXVBS6P7GZR06` · health ok 2026-09-22T09:06Z |
| Scoreboard stamp | `6c20024` on `cursor/spend-trust-recurring` (docs only, no second Fly) |
| Live | **PARKED** |
| ShopifyQL / `read_reports` | **HOLD** — not a craft blocker |
| Smoke | waived |
| Next craft already named | Rank 5 same-clock compare · `#157` · `cursor/same-clock-compare-5bc6` |
| After that | Rank 6 · third-order ticket and wait · Customers → Growth |
| Prior notes | `#148`–`#151` scouts · `#152` cook queue. Floor, not a finish. Do not re-run that wave as the same four files. |

**In flight (do not rebuild, do not “find” as new):** through-this-clock vs same weekday last year, and a custom from/to vs those calendar dates last year. Reviewer already FAILed a double-painted clock sentence and a quiet day painted as “still loading.” Fixes are on that branch. Conductor merges + one Fly only after PASS.

**Model:** inherit the Conductor. Do **not** pin `grok-4.7-xhigh` (that slug was rejected). Do not pin Grok 4.6.

Granola is not authenticated in this environment. Do not stall for meeting notes.

---

## What “enterprise” means on this pass

A multi-million-dollar Shopify store keeps **$39/store/month** after the first week because:

1. The question is on the **open lane** of a painted tab (not a settings wall, not a sixth tab, not a badge).
2. The empty tells the **truth** (dash / not on file / still syncing — never a fake $0, never 0×, never a SAMPLE catalog on Live).
3. The **phone** (Admin iframe ~390px, `max-width: 36rem`) can read the primary number.
4. A **teammate** can copy one sentence that names Shopify Total Sales and the window, without a Meta login.
5. A **Saturday operator** at 2:55pm can see through-this-clock and the period they manage, not last year’s evening and not a 90-day board when they picked this month.

Enterprise is not a visual refresh, a longer Settings page, or “we already have LTV.” It is jobs a $5M store still cannot do on this desk.

---

## Locks (breaking these is REFUSE, not a find)

- Painted IA: Overview → Orders → Customers → Spend → Goals. Growth and LTV are Customers chips. Never a sixth tab.
- One plan: $39/store/month after a 7-day trial. No Free plan. No GMV / order ladder.
- Total ROAS = Shopify Total Sales ÷ entered spend, or **—**. Never 0×. Never a fake $0.
- Product titles are not stored. SAMPLE must not invent a catalog Live cannot store.
- Guests out of returning. Missing last year is not on file, never $0. Codes are names, not percents (`WELCOME10` is not 10%).
- Refuse: COGS / P&L / gross profit as hero, pixels, MTA, “true ROAS,” sessions/visitors, Amazon, Recharge/Skio MRR, invented stars/installs/metrics, Live unpark, Partner Submit, outbound review sends, person name/email/city, industry benchmarks, example-store figures from competitor blogs.
- Do not Fly from `marketing-mix-model` on `cursor/clean-revamp-v8`. App URL stays `https://mcfly-analytics.fly.dev`.
- One writer per surface. Reviewer PASS before merge + **one** Fly. Fewer bigger PRs. A Fly requires a new merchant sentence.

---

## Already on the tip — recycling is a scout FAIL

Do not list these as discoveries. Do not recook them.

- Morning sentence (v410) · period-total hero + phone year cards (v411) · live catalog empty tells the truth (v412) · returning mix uses the stored book (v413) · Orders intelligence follows the picked period, code dollars, returns climbing, demo mounts the same board (v414)
- Promo LTV, source LTV (web / POS / Shop), spend paste, empty spend as —, deleted day as $0, unique SAMPLE buyers on the paste book (v409)
- Starter / open-lane LTV (v408)
- `OrderFact` already has `unitCount`, `discountCode` (code only), `sourceName`, `grossAmount`, `amount`, `lifetimeOrders`. Day totals go back five years. Order rows do not.

---

## Known still-PASS — not “new finds”

The 2026-09-22 cook queue already named these. A scout that only restates this list **failed**. You may add file-level depth and a sharper merchant sentence. Your **quota of new finds is on top of this list**, not this list with new adjectives.

Still uncooked, already PASS in `#152`:

1. Phone chips + full-price vs discounted peek (`mcfly-desk.css`; Orders `calc(33.333% - 0.32rem)` lock stays until that cook).
2. DeskLane chip does not open the fold (`DeskLane.tsx`: `useState(fold ? defaultOpen : true)` never follows a later `defaultOpen`). Depth, `?panel=spend-add`, Overview weekday fold.
3. Payback prints a day count from a day-0 `$0` anchor (`cash-payback.ts`). Show the anchors or stop printing the day count. Refuse causal / ads-manager payback.
4. History banner disagrees with the crawl (`UnlockFullHistoryBanner.tsx` says 24 months for trial; `LIVE_UNPAID_INGEST_DAYS = 90`). Do not unpark Live. Do not add a history SKU. Flat $39 stays one plan. `uninstall-friction.test.ts` currently **requires** “24 months of orders.”
5. Name source names already inside Other (`classifyOrderSource` — draft invoices, subscription renewals). Do not recook source LTV. Do not claim POS drawer tape.
6. Compete rows 2–8: this month’s sales by buyer age; new $ vs returning $ vs the same quarter last year; later orders still on a discount by starter class; next wait after they already came back; this year’s class beside last year’s at day 90; ticket by month of life; which starter month gave the most back by day 90. 8-buyer floor. Guests out. Thin side is —. Do not paint example-store figures.
7. Rank 6 (queued, not started): 1st / 2nd / 3rd / 4th+ ticket, reach, wait. Seal at ≥8 buyers. `customers-analytics.ts`, `GrowthScoreboard.tsx`, `CustomersGrowthSection.tsx`.

HOLD remains HOLD until the fact is on the order or Marty takes the call: refund processing date, country code, subscription-checkout bit, gift-card product flag, automatic discount titles, product title/SKU column, order rows past 24 months, product subtotal vs Total Sales, Whale → Admin door, million-order crawl as shipped, POS drawer tape, VAT-out, Stripe/PayPal time zones, ShopifyQL.

REFUSE stays refuse. Craft hole already named: `demo.goals.tsx` still prints “At {n}% profit margin” and SAMPLE Customers still paints “Kept after margin.” The later touch is **delete that sentence**, not invent COGS.

---

## Surfaces last wave barely touched (hunt here first)

Last queue lived on Overview, Orders, and the Customers mix. A scout that never opens Spend, Goals, LTV boards, Settings, the public `/demo` vs Admin `/app` pair, or a 36rem iframe **failed**.

Walk all of these as the owner of a store doing **real volume** (hundreds of thousands this month, not Snowdevil toy dollars):

| Surface | Start files |
| --- | --- |
| Overview | `OverviewFirstViewport.tsx`, `OverviewSalesChart.tsx`, `OverviewYoyCards.tsx`, `overview-first-viewport.ts`, `MorningHabitStrip.tsx` (read-only) |
| Orders | `OrdersIntelligence.tsx`, `orders-intelligence.ts`, `desk-sales-page.server.ts`, weekday/hour lane |
| Customers | mix, Growth scoreboard, LTV boards (`LtvFirstProductDrivers.tsx`, `LtvPromoBoard.tsx`, `LtvProductBoard.tsx`, whales) |
| Spend | `app.spend.tsx`, `CpaPaybackDesk.tsx`, `cash-payback.ts`, paste, empty — |
| Goals | `app.goals.tsx`, `demo.goals.tsx`, `OrderHistoryGoalsBoard.tsx`, forecast |
| Phone | `mcfly-desk.css` `@media (max-width: 36rem)` and `430px`, `desk-phone-layout.test.ts` |
| SAMPLE vs Live | `demo.*`, `public-sample-*.ts`, `UnlockFullHistoryBanner.tsx`, `live-unpark.ts` |
| Copy | `morning-habit.ts`, `SlackInsightCard.tsx`, boards that paint a sentence but do not copy |
| Site / listing | only if the public sentence disagrees with the desk. Do not invent reviews or installs. Pages deploy is Marty. |

---

## Hunt questions a lazy scout will skip

Answer with **file evidence**. “Looks fine” is not an answer.

1. Which primary numbers on Orders, Customers, Spend, and Goals still wrap, truncate, or hide at 36rem the way Overview year cards used to?
2. Where does SAMPLE paint a number Live cannot store (catalog was one; margin on Goals is another; are there more)?
3. Where does copy say 24 months, paid history, or “already on this desk” while unpaid ingest is 90 closed days?
4. Where does a chip, query param, or `defaultOpen` fail to open the lane the merchant tapped?
5. Where is a day count, percent, or ROAS interpolated through a fake $0 (payback is one; are there more)?
6. Where is a code, a source name, or a lifetime treated as a percent, a channel, or a zero?
7. What can a second seat copy besides the morning strip? What sentence does a buyer-ops teammate need on Growth / LTV that still is not copyable?
8. For a quiet Saturday: $0 today with last year not on file — which other boards still say “loading” or paint a finished zero for a missing book?
9. Compete rows 2–8: which of those jobs does the **current tip code** actually fail, with the function that returns the wrong grain?
10. A custom Goals year vs this month: does the forecast, the implied spend ceiling, and the year board name Shopify Total Sales and refuse a fake $0 when spend is empty?
11. Whales: `Whale ${i+1}` with no Admin door is HOLD. What **can** the whale board already say from stored lifetime and ticket that it currently hides?
12. Other source names already on `sourceName` — which strings land in Other today, and what typical order is missing?
13. Public `/demo` vs Admin `/app`: which boards mount on one and not the other (Orders intelligence was one; clock compare was another)?
14. `unitCount`, `grossAmount`, `discountAmount`, `lifetimeOrders` — which enterprise question do they already unlock that no board asks?
15. What breaks when this month is hundreds of thousands and last year is not on file — not $0, not 0%, not a squeezed nowrap dollar?

---

## Fleet (five read-only scouts, then one writer)

Scouts are **read-only**. Each owns **one new file**. No `app/` edits. No Fly. No shared file. No `gh pr create` (403). Push the branch; the Conductor opens the draft PR.

If a scout returns fewer than **eight significant new gaps** (not in “already on the tip,” not a restatement of known still-PASS without new evidence), the Conductor sends it back. Nits, comment fixes, CSS color, and “move the padding” are not gaps.

| Scout | Branch | File | Job |
| --- | --- | --- | --- |
| Desk truth | `cursor/next-desk-truth-5bc6` | `docs/ops/research/2026-09-22-NEXT_DESK_TRUTH.md` | Walk all five tabs + Growth/LTV chips on the v414 tip. Merchant sentences for jobs the stored book can already answer that no open lane asks. |
| Craft holes | `cursor/next-craft-holes-5bc6` | `docs/ops/research/2026-09-22-NEXT_CRAFT_HOLES.md` | Lies, empties, SAMPLE vs Live, banners, DeskLane, payback $0, margin sentences, demo/admin mount gaps. File + line evidence. |
| Compete | `cursor/next-compete-5bc6` | `docs/ops/research/2026-09-22-NEXT_COMPETE.md` | What Lifetimely, Peel, Triple Whale, TrueProfit, Putler, Repeat Customer Insights, and **native Shopify Analytics** still sell from **order history** that Mcfly does not. Cite 2026 public pages. Do not paste their example-store numbers. Rows 2–8 are known; find jobs **beyond** them. |
| Operator | `cursor/next-operator-5bc6` | `docs/ops/research/2026-09-22-NEXT_OPERATOR.md` | Public Shopify community / Reddit / app-review complaints from operators of **real volume**. New threads. Reciting tonytapay / Kayleigh / Wing-roro / petgrocer / MagnumFonseca as the whole note is FAIL. |
| Volume desk | `cursor/next-volume-desk-5bc6` | `docs/ops/research/2026-09-22-NEXT_VOLUME.md` | Be the Saturday operator of a $5M–$10M store: phone, teammate copy, quiet day, six-figure month, thin last year, unpaid 90 vs paid 24, whale list, million-order honesty (coverage line, not a new crawl). |

Then **one synthesizer** on `cursor/next-cook-queue-5bc6` writes `docs/plans/2026-09-22-enterprise-next-queue.md`.

The synthesizer:

- Reads all five notes plus `#152` (old queue).
- Collapses shared surfaces into one cook.
- Ranks **significant** ships only. Each ship is one merchant sentence a $5M store cannot say today.
- **Does not restart ranks 1–6.** Rank 6 stays next after same-clock PASS. This queue is **what comes after rank 6**, plus any still-PASS item that is now unblocked, in a real order.
- Top of this new list becomes the scoreboard “Next significant ship” only after rank 6 lands (or Marty skips it). Until then the scoreboard stays same-clock, then rank 6.
- Nits, SoT stamps, and re-audits of a hole already on the tip do not make the list.

---

## How a scout works

1. `git fetch origin cursor/spend-trust-recurring` and branch from that tip (`6c20024` or later). If same-clock has merged, use the new tip and do not list that compare as missing.
2. Read `docs/ops/SCOREBOARD.md`, this megaprompt, `docs/plans/2026-09-22-enterprise-cook-queue.md` on `origin/cursor/enterprise-cook-queue-5bc6`, and the four 2026-09-22 notes as a **floor**.
3. Read the tip code. Walk Overview, Orders, Customers (Growth, LTV), Spend, Goals. Public `/demo` and Admin `/app`. 36rem and desktop.
4. For every gap: merchant sentence, tab/chip, files, why a $5M store cares, **why it is not a nit**, PASS / HOLD / REFUSE.
5. PASS = one cook can ship it without a new scope, a new tab, or a lie.
6. Open nothing. Push the one markdown file. Return: branch, SHA, **eight or more new sentences**, and the ones you refused.

Conventional commit: `docs: …`. Do not commit `.superpowers/`.

---

## Conductor after the five notes exist

1. Open five draft PRs with ManagePullRequest (base `cursor/spend-trust-recurring`). Do not put cursor.com/agents URLs in PR bodies.
2. Spawn the synthesizer. Open its draft PR. **Do not merge research PRs** unless asked.
3. Do **not** start a new craft while `#157` is ungraded or rank 6 is the named next ship. One implementer at a time.
4. After rank 6 PASS + Fly, stamp the scoreboard with **rank 1 of the new queue** only. Then one cook.

Do not fan four craft agents onto `mcfly-desk.css`, `customers-analytics.ts`, `DeskLane.tsx`, or Overview.

---

## Kill list

- Do not report the niche is owned. Do not write “solid,” “enterprise-ready,” “the desk is complete,” or “only nits remain.”
- Do not stop because morning habit, period total, catalog empty, new-buyer truth, or the month board exist.
- Do not propose COGS, pixels, sessions, Amazon, Recharge, Slack product, or a sixth tab as the enterprise leap.
- Do not invent revenue, review counts, install counts, or Mcfly stars.
- Do not execute `docs/plans/2026-09-22-shopifyql-wait-queue.md` (stale; plans v407/v408 as future).
- Do not unpark Live. Do not Partner Submit. Do not send review asks.
- Do not open a second cook beside same-clock or rank 6.
- Do not Fly a nit.

---

## Significant vs nit (grade the scouts with this)

**Significant:** a $5M operator changes what they do on Saturday, or stops distrusting a number, or can copy a sentence their operator Slack needs. File evidence. One merchant sentence.

**Nit:** padding, eyebrow synonym, icon, comment, “add a tooltip,” restating a shipped rank, restating known still-PASS with no new file, a color pass, a sixth tab to look serious.

A Fly requires a new merchant sentence. Copy nits and SoT stamps do not deploy.
