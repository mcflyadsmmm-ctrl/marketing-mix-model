# W1 ship-gate + SAMPLE / `?shot=1` hygiene — 2026-07-29

**Lane:** Wave 1 App Store survival · reject hygiene (1.1.4 factual info)  
**Religion:** Cash Total ROAS = sales ÷ spend · no pixels / MTA / TW clones · no Fly/Pages deploy

---

## Gate results

| Script | Exit | Notes |
| --- | --- | --- |
| `bash scripts/agent-ship-gate.sh` | **0** | 350 unit tests (38+13+299) · typecheck · build · health `db up` · religion advisory OK |
| `bash scripts/mcfly-compliance-spotcheck.sh` | **0** | toml topics · scopes · App URL ≠ mcflyads.com · compliance + uninstall webhooks · no shop-domain install form · sales PII guard · CSV spend · `/health` |

Re-ran after SAMPLE copy hygiene — both still **PASS**.

---

## App Store 1.1.4 — SAMPLE desk vs `?shot=1`

### Intended split

| Mode | Purpose | Labeling |
| --- | --- | --- |
| SAMPLE **OFF** | Reviewer smoke / live store | No SAMPLE chrome; live sales copy (or empty setup) |
| SAMPLE **ON** + normal UI | Practice desk | `SampleDeskBanner` + `mcfly-desk--sample` |
| SAMPLE **ON** + `?shot=1` | Listing captures only | Banner hidden (quiet chrome); **must still say SAMPLE** |

Demo ops copy already: leave SAMPLE off for App Store review smoke; use `?shot=1` only after SAMPLE on for listing shots.

### Spot-check findings

| Surface | Before | Verdict |
| --- | --- | --- |
| Overview / Allocation / Goals / LTV | Ctx `· sample data` + freshness `SAMPLE preview` + CSS corner badge `.mcfly-desk--shot.mcfly-desk--sample::after` | OK — banner hide is intentional |
| **Spend** `?shot=1` | Banner hidden; CSS badge only; ctx showed bare period (no SAMPLE word) | **Gap** — could read as live spend UI |
| **Settings** `?shot=1` | Banner hidden; CSS badge; no SAMPLE chip in ctx | Soft gap (margin is real; SAMPLE desk still on elsewhere) |

### Minimal agent fix (this tick)

- Spend ctx: `{period} · SAMPLE` when sample desk enabled (survives shot mode)
- Settings: `SAMPLE desk on` chip when sample enabled (survives shot mode)
- Overview / Allocation / Goals / LTV: ctx `· sample data` → `· SAMPLE` (caps match product voice)

CSS listing badge retained — not removed.

---

## Remaining blockers (not agent-fixable)

| Item | Owner |
| --- | --- |
| W1-7 Listing shots + Partner Distribution → Submit | **HUMAN_GATE** |
| Reviewer path: SAMPLE **OFF** on test store before submit | **HUMAN** ([`SUBMIT_NOW.md`](../SUBMIT_NOW.md)) |
| Partner MFA / App Store submit click | **HUMAN_GATE** |

No typecheck/test/build failures this tick. No deploy.
