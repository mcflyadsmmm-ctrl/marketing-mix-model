# W1-5 Webhook ACK + idempotency — 2026-07-29

## Routes

| Route | ACK path | Idempotency |
| --- | --- | --- |
| `webhooks.orders.tsx` | Mark dirty day + enqueue `reconcile_sales_day` — no GraphQL in webhook | `recordWebhookDelivery` before enqueue |
| `webhooks.compliance.tsx` | Fast DB; cohort rebuild **enqueued** (`recompute_cohort_facts`) | `recordWebhookDelivery` before work |
| `webhooks.app.uninstalled.tsx` | Session + shop cascade delete | Claim then work; ledger purged with shop (replays re-run idempotent deletes) |
| `webhooks.app.scopes_update.tsx` | Scope string update | `recordWebhookDelivery` |

## ACK &lt;5s

- Orders: never paginate Admin GraphQL in the webhook.
- customers/redact: `recomputeCohorts: false` + job queue (was the slow risk).
- customers/data_request: Level-1 OrderFact package only (no CRM).

## Evidence

- Compliance + uninstall + scopes wired to `webhook-delivery.server.ts`
- Job handler `recompute_cohort_facts` in `job-runner.server.ts`
