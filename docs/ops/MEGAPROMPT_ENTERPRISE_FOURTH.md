# Megaprompt — enterprise fourth pass (cook the holes)

Paste this into the Conductor. **The desk is not finished.** Three megaprompts already ran. They found more than sixty Saturday jobs a $5M store still cannot do. Reciting v424, or that list, as “enterprise-ready” is the failure this pass exists to stop.

Marty: recently it has felt like agents think everything is perfect. It is not. **This pass ships.** A fourth wave of scouts that only restates `#174`–`#176` / `#178`–`#179` is lazy. The improvements are already named. Cook them. If a scout returns fewer than ten **new** gaps **beyond** the 14-rank third queue, it stopped too early — send it back. Nits do not count.

---

## Tip (floor, not a finish)

| Item | State |
| --- | --- |
| Fly | **v424** · merge `3346136` (#177 whale ticket) · image `deployment-01M34QNHWPNPZF21MYB5SQH576` · health ok 2026-09-22T14:19Z |
| Scoreboard stamp | `e71ea55` on `cursor/spend-trust-recurring` (docs only, no second Fly) |
| Live | **PARKED** |
| ShopifyQL / `read_reports` | **HOLD** — not a craft blocker |
| Smoke | waived |
| Next craft already named | Rank 9 Orders step mix · `cursor/orders-step-mix-5bc6` · **in flight** · no Fly until Reviewer PASS |
| After that | Third-queue **rank 1** buyer-life honesty · `cursor/buyer-life-honesty-5bc6` · plan `#180`-to-be / `docs/plans/2026-09-22-enterprise-third-queue.md` |
| Findings already on disk | `#173` third megaprompt · `#174` compete · `#175` operator · `#176` desk truth · `#178` volume · `#179` craft holes. Floor, not a finish. |

**In flight (do not rebuild, do not “find” as new):** this month’s Shopify Total Sales by 1st / 2nd / 3rd / 4th+; first-time vs returning ticket; tax/shipping slice; period concentration; new vs returning hour; discount `$` vs last September; kept share vs last year; first vs returning discount depth; dollars per unit; `$0` reship count. Unknown lifetime its own bar or —. 8-buyer seal. Guests out. Conductor merges + one Fly only after Reviewer PASS.

**Model:** inherit the Conductor. Do **not** pin `grok-4.7-xhigh` (rejected). Do not pin Grok 4.6.

Granola is not authenticated in this environment. Do not stall for meeting notes.

---

## What “enterprise” means on this pass

A multi-million-dollar Shopify store keeps **$39/store/month** after the first week because:

1. The question is on the **open lane** of a painted tab (not a settings wall, not a sixth tab, not a badge).
2. The empty tells the **truth** (dash / not on file / still syncing — never a fake `$0`, never `0×`, never unknown lifetime stuffed into first-time `$`).
3. The **phone** (Admin iframe ~390px, `max-width: 36rem`) can read a **six-figure** primary number. Snowdevil `$68k` does not prove this.
4. A **teammate** can copy one sentence that names Shopify Total Sales and the window.
5. A **Saturday operator** at 2:55pm can see through-this-clock and the period they manage, not last year’s evening, not a UTC mix chart, and not a year chip on a 90-day unpaid book.

Enterprise is not a visual refresh, a longer Settings page, or “we already have LTV.” It is jobs a $5M store still cannot do on this desk.

---

## Locks (breaking these is REFUSE, not a find)

- Painted IA: Overview → Orders → Customers → Spend → Goals. Growth and LTV are Customers chips. Never a sixth tab.
- One plan: $39/store/month after a 7-day trial. No Free plan. No GMV ladder.
- Total ROAS = Shopify Total Sales ÷ entered spend, or **—**. Never `0×`. Never a fake `$0`.
- Product titles are not stored. SAMPLE must not invent a catalog Live cannot store.
- Guests out of returning. Missing last year is not on file, never `$0`. Codes are names, not percents (`WELCOME10` is not 10%).
- Refuse: COGS / P&L / gross profit as hero, pixels, MTA, “true ROAS,” sessions/visitors, Amazon, Recharge/Skio MRR, invented stars/installs/metrics, Live unpark, Partner Submit, outbound review sends, person name/email/city, industry benchmarks, example-store figures from competitor blogs.
- Do not Fly from `marketing-mix-model` on `cursor/clean-revamp-v8`. App URL stays `https://mcfly-analytics.fly.dev`.
- One writer per surface. Reviewer PASS before merge + **one** Fly. Fewer bigger PRs. A Fly requires a new merchant sentence.
- Do not execute `docs/plans/2026-09-22-shopifyql-wait-queue.md` or `#137` / `#145`. ShopifyQL stays HOLD.
- Do not change `LIVE_UNPAID_INGEST_DAYS = 90`. Do not edit `fly.toml` scopes.

HOLD remains HOLD: refund processing date, country, subscription-checkout bit, gift-card product flag, automatic discount titles, product title/SKU, order rows past 24 months, Whale → Admin door, million-order crawl as shipped, POS drawer tape, VAT-out, Stripe/PayPal time zones, ShopifyQL.

---

## Already on the tip — recycling is FAIL

v408 starter LTV · v409 promo/source LTV, spend paste, empty spend —, deleted day `$0` · v410 morning · v411 period-total phone · v412 catalog empty · v413 returning mix from the book · v414 Orders intelligence on the picked period · v415 same-clock · v416 third-order steps · v417 Goals honesty · v418 phone Goals stack · v419 Spend pair · v420 book coverage 90 vs 24 · v421 Goals year clock · v422 DeskLane later open · v423 quiet-back `$` + copyable first-time `$` · v424 whale ticket / remainder / Champion flow.

`OrderFact` already has `unitCount`, `discountCode`, `sourceName`, `grossAmount`, `amount`, `lifetimeOrders`. Day totals go back five years. Order rows do not.

---

## Already ranked — not “new finds”

`docs/plans/2026-09-22-enterprise-third-queue.md` on `cursor/enterprise-third-queue-5bc6`. Fourteen cooks. A scout that restates this list **failed**.

| Rank | Sentence | Branch | State |
| ---: | --- | --- | --- |
| 9 | This month `$` by 1st/2nd/3rd/4th+; first vs returning ticket; tax/shipping; concentration; hour; discount `$` vs last September; kept share; discount depth; `$`/unit; `$0` reships | `cursor/orders-step-mix-5bc6` | **In flight** |
| 1 | Unknown lifetime stuffed into first-time `$`; Shopify `numberOfOrders` is today; till LTV skips `lifetimeOrders > stored`; mix still UTC | `cursor/buyer-life-honesty-5bc6` | **Next after rank 9 PASS + Fly** |
| 2 | Goals YTD % look-ahead; typed `$0` vs —; 0% bar on null actual; `/demo/goals` habit board vs `$0` plan | `cursor/goals-leftover-honesty-5bc6` | queued |
| 3 | Settings “whole desk is on”; explorer This year / 1 year / All and CPA YTD on a 90-day unpaid book | `cursor/unpaid-year-leftover-5bc6` | queued |
| 4 | Truncated today still a finished day on Orders typical + Customers returning `$`; last year missing never said on Customers first fold | `cursor/truncated-today-leftover-5bc6` | queued |
| 5 | Spend import host calendar; coverage host-local; SAMPLE import 35% BE; Online ROAS all-spend denominator; no weekday grain | `cursor/spend-clock-roas-5bc6` | queued |
| 6 | Coverage hash opens Add-spend; Mix close still opens weekday (second wrap class) | `cursor/wrong-lane-leftover-5bc6` | queued |
| 7 | LTV 30/90/365 three-up at 36rem; LTV triangle nowrap; Orders weekly eight-column nowrap | `cursor/phone-ltv-orders-5bc6` | queued |
| 8 | Two first-90 numbers; `cohortLtvCurves` still in Depth; SAMPLE estimate always open; payback day-0 `$0` interpolation | `cursor/first-90-honesty-5bc6` | queued |
| 10 | Paid typical; one-item vs 2+ `$`; discounted till share; POS hour `$`; weekday `$`; 7×24 grid; copy Orders typical | waits for rank 9 `app.orders.tsx` | queued |
| 11 | Guest `$` + Other names + POS vs online returns, plus new vs returning `$` by source and overlap people | `cursor/guest-source-returns-5bc6` (absorbed bigger) | queued |
| 12 | Compete 2–3 + annual base + MoM + same-clock returning `$` + typical week + POS vs online `$` | Overview | queued |
| 13 | Compete 4/6/7/8 + launch-week class + orders per month of life | LTV chip | queued |
| 14 | Wait histograms past the second; last-month actives who came back; wait by first source; lifetime A/B/C | Growth | queued |

---

## How this pass works (cook first)

1. **One implementer.** Orders mix until PASS + Fly. Then rank 1 buyer-life. Then 2, 3, 4… Skip rank 9 if Orders mix already shipped that sentence. Do not fan four agents onto `mcfly-desk.css`, `customers-analytics.ts`, `orders-intelligence.ts`, or `app.goals.tsx`.
2. **One Reviewer.** Read-only. Omit-path FAIL class (required props, not a file-level `toContain`). `tsc` `Cannot find name` is FAIL. Nits do not deploy.
3. **One Fly** after PASS. A Fly requires a new merchant sentence. Scoreboard stamp is docs only — no second Fly.
4. **Do not merge research PRs** (`#173`–`#176`, `#178`–`#179`, this megaprompt, the third queue) unless asked.
5. **Do not start a second cook** beside Orders mix.

Scouts are **off** until rank 1 of the third queue has shipped, **or** a Reviewer FAIL needs a re-audit. Then at most **two** read-only scouts (craft leftover + volume leftover). Each owns one new file. Quota **ten new** gaps not in the table above. Reciting ranks 1–14 is FAIL. No `app/` edits. No Fly. No `gh pr create`.

---

## Hunt questions (only if those two leftover scouts run)

Answer with **file evidence** on the **then-current tip**. “Looks fine” is not an answer. Do not answer with a rank already in the table.

1. After Orders mix and buyer-life, where is unknown `lifetimeOrders` still stuffed into 1st / new `$` / CPA?
2. Which six-figure primary numbers still hide at 36rem **after** rank 7 (LTV windows + Orders ledger)?
3. Which boards still use host `Date` / UTC **after** rank 1 mix clock and rank 5 Spend import clock?
4. What can a second seat still not copy on Spend leftover / Settings / LTV **after** ranks 2, 4, 8, 10 copy controls?
5. Quiet Saturday `$0` today + last year not on file — which **remaining** board still paints a finished zero **after** rank 4?
6. SAMPLE overlay leftover **after** rank 5 kills import 35% BE and rank 8 kills the LTV estimate always-open — is there another?
7. Billing bounce / trial vs 90 vs `$39` leftover **after** rank 3.
8. Native Analytics / Lifetimely / Peel **2026** job **not** already compete 1–16 or ranks 10–14.

---

## Kill list

- Do not report the niche is owned. Do not write “solid,” “enterprise-ready,” “the desk is complete,” “only nits remain,” or “the last queue covered it.”
- Do not stop because v408–v424 exist. Do not stop because sixty findings exist uncooked.
- Do not propose COGS, pixels, sessions, Amazon, Recharge, Slack product, or a sixth tab as the enterprise leap.
- Do not invent revenue, review counts, install counts, or Mcfly stars.
- Do not execute the ShopifyQL-wait queue.
- Do not unpark Live. Do not Partner Submit. Do not send review asks.
- Do not open a second cook beside Orders mix.
- Do not Fly a nit.
- Do not change the 90.

---

## Significant vs nit

**Significant:** a $5M operator changes what they do on Saturday, or stops distrusting a number, or can copy a sentence their operator Slack needs. File evidence. One merchant sentence.

**Nit:** padding, eyebrow synonym, icon, comment, restating a shipped rank, restating the 14-rank table with no new file, a color pass, a sixth tab to look serious, `site/pricing.html` 24 months, Mix-close weekday **unless** rank 6 has not shipped.

A Fly requires a new merchant sentence. Copy nits and SoT stamps do not deploy.
