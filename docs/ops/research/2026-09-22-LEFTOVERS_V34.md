# Leftovers v34 → v35 cook — 2026-09-22

**Live probed:** https://mcflyads.com · `mcfly-version` **v34** Pages `ad054108`  
**Git cook:** `cursor/leftovers-v34-5bc6` from `cursor/site-world-class-5bc6` · spine **v35**  
**Did not:** New landers. App Store listing rewrite (Marty Save #192). Pages deploy. Fly.

## Needles

| Needle | Live v34 (before this cook ships) | Git after cook |
| --- | --- | --- |
| `og-cash-mer.jpg` | **200** asset (`/assets/brand/og-cash-mer.jpg`). No 200 HTML `og:image` still points at it. | File **deleted**. `_redirects` 301 → `og-analytics.jpg`. Spine 200 pages have no reference. |
| beat native Analytics | Clean on 200 spine | Clean |
| full-access / Trial includes 24 months | Clean on 200 spine | Clean. `site-go-live` still bans both. |
| Polar $1,020 (invented) | **Dirty on home** (“We do not invent Polar $1,020”) | Removed. Home cites Polar list **$750/mo** + listing “from” URLs. |
| Monday Close / CashClose / monday-board / Guests stay out | Clean on 200 spine. `/monday-close` **301 /** | Body leftovers remain in parked `monday-close.html` (OK). Canonical → `/`. `noindex`. |
| mds-made-easy 200 | Pretty + `.html` **301 /** today | Still 301 (`/mds-made-easy/* / 301`). Canonical → `/`. `noindex,nofollow`. HTML files stay in git. |
| break-even-roas-calculator canonical indexed | Pretty URL **301 /pricing**. HTML still **self-canonical** if a crawler ever saw 200. | Canonical + `og:url` + schema `url` → `/pricing`. `noindex,follow`. `.html` 301 `/pricing`. |
| Harbor Home Co / Northline $98,500 / 4.19× on home | Clean on `/` | Clean on `/`. Numbers remain in parked lab/Custom HTML (OK). |
| @ 40% / 2.50× as a pledge | Home shows SAMPLE **2.50×** with “type margin in Settings” — not a 40% pledge | Unchanged SAMPLE lock. `/product` still refuses 40% pledge. |
| Custom inquire / packages | Clean on 200 spine. Parked Custom HTML still inquires | Parked only. Canonical → `/`. `noindex`. |
| sessions/visitors as a product claim | `/about` refuse: **No sessions/visitors**. `llms-full` refuse list | Kept as refuse, not a claim. |
| Slack bot / Monday.com sync | Home: “not a Slack bot” (copy, not a bot) | Kept as denial. No Monday.com sync on spine. |
| Sample\|Live toggle | Support FAQ keeps exact `no Sample\|Live toggle` | **Kept** (tests require it). |
| listing vs site | Missing honesty line | One line on `/` + `llms.txt`: listing card is still spend-first until founder Save. Listing not rewritten. |

## `_redirects` confirmed

```
/mds-made-easy/* / 301
/break-even /pricing 301
/break-even-roas-calculator /pricing 301
/mer-calculator /pricing 301
```

Added: `.html` calculator hops, `/assets/brand/og-cash-mer.jpg` → `og-analytics.jpg`.

## After this cook ships vs parked git

**Still dirty on LIVE until Pages v35 uploads:** home Polar `$1,020` sentence, public `og-cash-mer.jpg` 200, calculator HTML self-canonical if the file is fetched, `lab.html` `index,follow`.

**Only dirty in git parked 301s (OK):** `lab.html`, `custom-analytics.html`, `monday-close.html`, `mds-made-easy/**`, `spend-sales-audit.html` and sibling Custom landers — Northline `$98,500` / `4.19×`, Custom inquire, Monday Close body copy. Pretty URLs already 301. Do not delete those files (sample-lock still reads `/lab`).
