# Status — Fly 237 Polar book cycle (2026-09-10)

## Why
Founder: app still felt super low quality after Fly 236. Full revamp: one report per tab, not a collage of widgets.

## What
- Overview: period → Shopify Total Sales hero → quiet glance → one sentence → door links → Goals book → Marketing book → chart as a last drill (not a second dashboard).
- Duplicate titles gone. Sticky rail is period + as-of only.
- Goals / LTV / Spend use the same book language. LTV chrome drops cohort / till / ARPU.
- Paper CSS: Fraunces only on the sales hero value. Flattened sky cages. Explorer quieter.

## Gate
Ship-gate **PASSED** (810 tests, typecheck, build). Compliance spotcheck **PASSED**. `fly deploy -a mcfly-analytics --yes` exit 0.

Cloud Cursor VMs could not start: they clone `martysmithson04-alt/marketing-mix-model`, which this session cannot push. Cycle ran locally against `mcflyadsmmm-ctrl` `cursor/spend-trust-recurring`.

## Live
- Fly **237** · image `deployment-01M260PX8097RA31FR28G8BMYT`
- `/health` db up · `/app` `/auth/login` **200**
