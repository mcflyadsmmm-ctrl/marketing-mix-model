# Site v26 — punch list (other-Cursor v23 QA)

**When:** 2026-09-18 · America/Denver  
**Source:** other-Cursor list taken against **live v23**. Home was already **v25** contain/crops. This ship is leftover P0s + Amp 5/7 grid.

Inbox PNGs were **not** in this checkout (`docs/ops/inbox/shots/` missing). Geometry taken from the pasted list + live DOM.

## /demo P0

| # | Fix |
| --- | --- |
| 1 Ghost CTAs | `.btn.ghost` ink `#0a1221` + 1px border; Install + Pricing — $39/mo visible |
| 2 H1 under nav | deleted `brand-kicker`; `padding-top: calc(var(--nav-h) + 48px)` |
| 3 390 header | `site.css` `.nav { display:none }` was hiding Mcfly chrome; `body.demo-page header.nav { display:block }` + hamburger ≤720 |
| 12 Mint CTA | primary `#0a1221` / white |

Desk below the hero: not touched.

## Home (code)

| # | Status |
| --- | --- |
| 4–5, 9 cover crop | Already **contain** on v25 crops — kept |
| 6 Shot wins | `max-width:1240px` · `5fr / 7fr` · `gap:56px` · bleed right |
| 7 Flip shrink | `.feature-row--flip { 1.25fr / 1fr }` |
| 8 Same 02/04 | Residual — `/demo` has no Total ROAS / Customers tabs to export; rows use distinct micros |
| 10 Signal cards | Already removed on v25 (one idea + one well) |
| 11 Type | H1 clamp 40–52 · H2 30–40 · lead 18px · section 112px |
| CTA pair | Restored Install + Try the demo; `$39` line kept under |

## Gate

SAMPLE LOCK PASSED. H1 locked. Install → listing. Reviews **0**. Ads **NO**.

## Live

Pages production from non-git temp — `4a154f4d` · https://mcflyads.com `mcfly-version` **v26**.
