# Status — Fly 238 Tick C+D (2026-09-10)

## Why
World-class attack Phase 1: Marketing must work at $0 spend, and a forwarded recap must not title Total ROAS when spend is empty. Listing and mcflyads.com wait on Admin shots of this desk.

## What
- Marketing `s-page` heading is **Marketing**. At $0 spend: three doors + honesty; coverage/chart hidden until a day exists. After spend: Total ROAS hero + mix.
- Share card at $0 spend titles Shopify Total Sales. Empty spend is not 0×. With spend, first number is still Total Sales.
- Listing paste blocks in `docs/APP_STORE_LISTING.md` are sales-first (billboards + spend you added). Marty Submits; Cursor does not.
- Site v16 brief exists. Do not Pages-deploy until Phase 2 shots.

## Gate
Ship-gate **PASSED** (814 tests, typecheck, build). Compliance spotcheck **PASSED**. `fly deploy -a mcfly-analytics --yes` exit 0.

## Live
- Fly **238** · image `deployment-01M264F033QVYN8W59PTNG7P8X`
- `/health` db up · `/app` `/auth/login` **200**
