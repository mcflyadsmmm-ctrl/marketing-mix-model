# Status — Fly 236 book + drill-down (2026-09-10)

## Why
Founder: Orders/Buyers equal-card grids were pathetic. All tabs should look top-tier. More info is fine if it drills open.

## What
- Orders / Buyers / Timing: one hero + three-column sales clock (Orders) + native `<details>` rows. No `BookTile` zoo. Tick B numbers live in drills, not extra boxes.
- Overview: Total Sales stays the lead. Glance stats omit `—`. Duplicate “Overview” title gone. Report doors are links, not boxed teasers.
- Goals + Marketing snaps on Overview use the same book language. Settings stay Polaris.
- Percents are whole numbers. Guest / one-order / unknown returns or shipping rows are omitted, not fake 0.0% or boxed dashes.

## Gate
Ship-gate **PASSED** (804 tests, typecheck, build). Compliance spotcheck **PASSED**. `fly deploy -a mcfly-analytics --yes` exit 0.

## Live
- Fly **236** · image `deployment-01M25PWBX5QSFV2KEEMH2VSXAE`
- `/health` `{"ok":true,"db":"up"}`
- `/app` `/auth/login` `/privacy` `/pricing` `/support` `/terms` **200**
