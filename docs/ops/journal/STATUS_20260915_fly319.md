# Status — Fly 319 (2026-09-15)

## Why
Busy-shop OrderFact crawls must not seal a truncated day or paint incomplete sales as $0. Uninstall risk: a high-volume shop sees a finished book that is wrong.

## What
- Truncated closed-day crawls persist a GraphQL page cursor; next worker tick **continues**, not restarts.
- Page budget 25 → 40 (hard cap 80). Job requeues `{ incomplete: true }` instead of succeeded.
- CashTrustBanners: **Order history still loading** — incomplete sales are not $0.
- Overview + Shopify five (Customers / Growth / Orders / LTV) now pass `orderFactsTruncated` from `getOrderBackfillProgress`.
- `read_orders` ~60 days unchanged. No `read_all_orders`.
- Site in image: git HEAD + overlay `support.html` + `vs-attribution-suites.html` (no “listing pending”).
- Vitest subset **138** · typecheck pass · Desk lane full suite was **938** before the Overview wire.
- Fly **319** · image `deployment-01M2M9017M1WZ3F069FYY2BY1E`
- Reviews: **0** — not invented
- Ads: still NO

## Live
- https://mcfly-analytics.fly.dev · `/health` db up · `/app` 200 · `/auth/login` 200 · `/support` honest

## Human leftover
Admin SAMPLE smoke on Fly 319. Partner paste is Marty. Ads off.
