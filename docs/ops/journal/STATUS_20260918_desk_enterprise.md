# Desk Admin enterprise fold — 2026-09-18

**Lane:** Desk Admin Enterprise Fold · Cursor Grok 4.6  
**Bar:** 1600×900 well crop could sit next to a Lifetimely still without looking like a spreadsheet skin.  
**Religion:** empty = — · Overview still zero spend · greeting / operator copy untouched · no invent metrics · no commit / Fly.

## Landed

| Surface | Change |
| --- | --- |
| Overview first lane | Sales explorer moved into first `DeskLane` after YoY (listing shot 1 composition) |
| Peeks well | Hero + 3-up grid; founder skinny flex strip overridden on Overview |
| Handoff peeks | Own well; dropped `peeks-lead` so Days-to-second is not a fake full-bleed hero |
| YoY / chart wells | Taller tiles + raised chart min-height (`--mcfly-fold-chart-min`) |
| Total ROAS glance | Dark elevated tiles; lead ROAS uses hero type + accent |
| CertifiedScoreboard | `mcfly-well` + `--certified`; chips readable as listing shot 5 |

**Tokens raised:** `--mcfly-kpi-display` / `--mcfly-kpi-hero` + fold pad / tile mins in `mcfly-desk.css`.

## Verify

```bash
npx vitest run app/app/lib/overview-first-viewport.test.ts app/app/lib/overview-yoy.test.ts app/app/lib/spend-upload-findings.test.ts
```

## Lifetimely sit-next-to bar (honest)

| Criterion | Grade | Note |
| --- | --- | --- |
| Dark well first fold | B | Peeks + YoY + chart now share first lane; density raised |
| Hero hierarchy | B | Typical order / Total ROAS lead dominate numerals |
| Empty — | A | Unchanged religion |
| Phone @430 | B- | Grid collapses; chart still readable |
| Sit next to Lifetimely | **B- / pending Marty eyes** | Craft is denser than Fly 391 spreadsheet skin. Not Amp collage photography. Residual: no real Admin PNG compare yet; mix/share still below fold; soft ROAS light CSS still in file but well rules win when classes present |

**PASS for code gate.** Visual PASS still needs Marty Admin recapture after Fly ships this tree.
