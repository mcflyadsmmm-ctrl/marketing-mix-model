# Status — Fly 321 (2026-09-16)

## Why
Admin first paint showed **410 Gone**. Customers (and the other Shopify five) looked like a skeleton: leftover MTD chips and every `—` card was hidden.

## What
- Document 410 from `authenticate.admin` redirects to `/auth/opening` (200 + App Bridge), then client-navigates to `/app`. `.data` can still 410 once for the token handshake.
- Shopify five always paint the locked hero + KPI cards. Empty is **—**, not a blank lede.
- `showPeriod={false}` on Customers / Growth / Orders / LTV. Period chips stay on Total ROAS / Spend.
- New vs returning mix chart still renders when shares are 0.
- Image `deployment-01M2N86Q6M21BFZ4F6R081AG1E` · machines version **321**
- `/health` db up · embedded `/app?shop=` **302** `/auth/opening` (was **410**)

## Human
Settings → Sample data to see Harbor numbers. Partner listing still spend-led. Reviews **0**. Ads **NO**.
