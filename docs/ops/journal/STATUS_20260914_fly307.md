# Status — Fly 307 million-store desk (2026-09-14)

## Why
Overnight Conductor: ticks 1–3 of `docs/plans/2026-09-13-million-store-desk.md`. Overview is a sales scoreboard (no homework tabs). Marketing is the spend room after one typed day. `aMER` off merchant chrome.

## What
- Overview: Shopify Total Sales always; Total ROAS when spend > 0; window rail; signal cards; chart. `#mcfly-mix` etc. land home.
- Marketing empty: three doors. After one typed day: mix + vs last month + plan + Every day.
- `CashControlBoard.tsx` deleted. Loader still builds chips for the window rail.
- Merchant label: New sales ÷ spend (code key `amer`).

## Gate
`bash scripts/agent-ship-gate.sh` **PASSED** — mer-core 41 + mer-engine 16 + app **855** = **912** tests, typecheck, build, `/health` db up.

`fly deploy -a mcfly-analytics --yes` exit 0 from `cursor/spend-trust-recurring` (not `cursor/clean-revamp-v8`).

## Live
- Fly **307** · image `deployment-01M2FG2Y877YY8NXT0JAG3J90P`
- `/health` `{"ok":true,"db":"up"}` · `/app` **200**
- Site Pages still **v15**
- Reviews: **0** · Ads off

## Human
Admin smoke on a shop (Sample + Live). Listing paste. Partner Submit is Marty.
