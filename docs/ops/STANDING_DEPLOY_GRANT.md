# Standing founder grants

**Effective until founder revokes** (`revoke deploy` / `stop deploy`).

| Grant | Scope |
| --- | --- |
| **Fly deploy** | `fly deploy -a mcfly-analytics` after ship-gate PASS when app changes ship |
| **Pages deploy** | When site ticks land |
| **Autonomy** | Keep improving without waiting for per-turn `deploy app` |

**Still HUMAN:** Partner MFA, Distribution, PCD, App Store Submit, Meta/Google App Review, billing card, DNS.
