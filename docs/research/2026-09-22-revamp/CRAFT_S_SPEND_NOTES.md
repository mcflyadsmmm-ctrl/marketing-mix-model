# Craft S (v49) — Desk Spend

**Branch:** `cursor/craft-spend-v49`  
**Done bar:** First two scrolls = Total ROAS hero (entered spend honesty) → vs-prior compare → spend explorer with `<h3>`. Essay ledes removed from first fold. Mix, CPA, certified chips, dual-close, and pacing fold at `rank="more"` with `defaultOpen={shotMode}`.

- `SpendFirstViewport` — native `<section>` + overview-plane rhythm; Shopify Analytics contrast is SR-only (`SPEND_ANALYTICS_SR_LINE`).
- `SpendCompareGlance` — sales, spend, Total ROAS vs prior period when `metrics.deltas` on file.
- `SpendExplorer` on Spend routes uses `<h3 className="mcfly-chart__h">` for the chart masthead.
- Empty spend paints **—**, never 0×; add-a-day + paste stay in first lane when `emptyLiveSpend`.
- No `<s-section className>` on Spend paths.
