# Craft S (v47) — Desk Orders

**Branch:** `cursor/craft-orders-v47`  
**Done bar:** First two scrolls = typical-order hero (From orders) → compact vs-prior compare → weekday/hour chart with `<h3>`. Essay `mcfly-book__lede` removed. Sales clock + intelligence + frequency fold at `rank="more"`.

- `OrdersFirstViewport` uses native `<section>` + overview-plane rhythm; Analytics contrast is SR-only.
- `OrdersCompareGlance` — three KPIs vs prior; `OrdersTimingChart` — `mcfly-chart__h`, no visible timing essay.
- No `<s-section className>` on Orders paths.
