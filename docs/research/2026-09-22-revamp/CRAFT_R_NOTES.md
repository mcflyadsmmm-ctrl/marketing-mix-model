# Craft R (v46) — Desk Overview

**Branch:** `cursor/craft-v46`  
**Done bar:** First two scrolls = hero `$` (From orders) → compact YoY metrics list → one chart section. Mix / shareables / depth folded at `rank="more"`.

- YoY + chart use `s-section` headings; pending copy moves to screen-reader-only where it duplicated visible numbers.
- Order-book depth line stays in DOM via `overviewCoverageLine` but off the visible meta row.
- Chart masthead drops the multi-clause lede + pace bars; range label only.
