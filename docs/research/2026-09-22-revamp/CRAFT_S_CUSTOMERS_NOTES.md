# Craft S (v48) — Desk Customers

**Branch:** `cursor/craft-customers-v48`  
**Done bar:** First two scrolls = returning-$ hero (From orders) → same-window-last-year compare → mix chart with `<h3>`. Essay `mcfly-book__lede` removed from routes. Shareables + scoreboard moved to `rank="next"`.

- `CustomersFirstViewport` — native `<section>` + overview-plane rhythm; Shopify Analytics contrast is SR-only (`CUSTOMERS_ANALYTICS_SR_LINE`).
- `CustomersCompareGlance` — returning $, new $, dollars per buyer vs last year when on file.
- `CustomerMixChart` — `mcfly-chart__h` heading; quieter masthead meta.
- No `<s-section className>` on Customers paths.
