# Significant desk ships while ShopifyQL waits

**Tip:** Fly **v408** · `d34b40c` · Live **PARKED** · smoke **waived**  
**ShopifyQL / `read_reports`:** checklist F stays **HOLD**. Not a craft blocker.  
**Spawn gate:** `.cursor/skills/mcfly-cursor-optimizer/SKILL.md` — checker exits 0 before any Task.

One Fly per accepted ship. Builder does not merge. Reviewer uses `grok-4.7-xhigh`. Workers do not deploy.

## Already true — do not rebuild

| Fly | Ship | PR |
| --- | --- | --- |
| v407 | Spend paste cash trio | #138 |
| v408 | Open-lane starter value (promo up, Online/POS/Shop → LTV, Live `discountCode`) | #142 |

Also shipped and closed: returning $, cohort 30/90/365, days-to-second, product→LTV, whales, refunds honesty, predictive LTV, forecast, shareable cards, unpaid ~90d clamp, promo depth bands.

## Queue

| Fly | Ship | State | Why it is the next merchant win |
| --- | --- | --- | --- |
| v409 | Spend + Overview honesty | **#143 open** — Reviewer now | SAMPLE Cash CPA uniques buyers. Blank cells are not $0. Payback is an interpolated average. Overview ready-state ignores spend. |
| v410 | Morning habit | **Craft now**, disjoint files | One copyable morning sentence plus three named first wins (month close, who to save, set a target). Zero OAuth. |
| v411 | Mobile soft-dense pass | After v410 | Phone CSS exists. Only cook if a 390px pass still fails the primary KPI + one ActionCard. |
| — | Orders product × date | **HOLD** | `OrderFact` stores units, not product titles. No title crawl in this wait. |
| — | Discount titles, country/tag LTV | **HOLD** | Schema crawl / PCD L2. |
| — | Sessions / visitors | **REFUSE** | Out of niche. |
| — | Listing multi-store $39, Pages, Partner, review sends | **Marty** | No Submit, no send, no invented metrics. |

## v410 exclusive files

Craft may create and edit only:

- `app/app/lib/morning-habit.ts`
- `app/app/lib/morning-habit.test.ts`
- `app/app/components/MorningHabitStrip.tsx`
- `app/app/components/DeskBookPage.tsx`
- `app/app/components/OverviewMixForecast.tsx`
- `app/app/components/OrderHistoryGoalsBoard.tsx`
- `app/app/components/GrowthTt2Board.tsx`

Do not edit spend routes, `app._index.tsx`, `CustomersLtvSection.tsx`, `LtvSnapSection.tsx`, or `SCOREBOARD.md`. Those belong to #143.

## Locks

Painted IA Overview → Orders → Customers → Spend → Goals. Growth and LTV stay Customers chips. $39 flat. Empty stays `—`. Refuse COGS, pixels, MTA, GMV, a sixth tab, a Slack bot, and invented Partner metrics.

## After each PASS

Conductor re-runs the prompt `DONE` command, merges, then one `flyctl deploy --app mcfly-analytics --remote-only`. Scoreboard tip line moves to that version. No second Fly for a copy tweak inside the same ship.
