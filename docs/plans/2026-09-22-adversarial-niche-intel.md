# Adversarial niche intelligence wave (ShopifyQL wait)

> Conductor dispatches short-lived subagents. Smoke waived. Listing later (Marty). Review sends later (Marty). Live PARKED. ShopifyQL / `read_reports` = trust gate only — not a craft blocker.

**Goal:** Own the $39 Shopify order-history / LTV niche by forcing uncomfortable truth: where Mcfly is wrong, where competitors beat us, what top Shopify apps do that we can steal, which real operator pains we can still solve inside the painted desk, then one gap-fill cook queue.

**Not the goal:** A hundred craft PRs. Fan-out is for **readers**. One Scout synthesizer writes the board + plan. Craft still cooks one ADD at a time after Reviewer.

## Five agents (plus Conductor)

| Agent | ONE JOB | Writes | Output path |
|-------|---------|--------|-------------|
| **Critical Audit** | Rip app + site + ops docs for mistakes, honesty bugs, stale SoT, empty-state lies, SAMPLE/Live confusion | Docs only | `docs/ops/research/2026-09-22-CRITICAL_AUDIT.md` |
| **Compete Why-Better** | Argue why Lifetimely, Triple Whale, Peel, TrueProfit, Putler, and native Analytics are **better** — with public URLs | Docs only | `docs/ops/research/2026-09-22-COMPETE_WHY_BETTER.md` |
| **Top Shopify Apps** | What category-leading Shopify apps actually do (IA, pricing, empty states, listing craft, habit loops) that Mcfly can steal **without** leaving the niche | Docs only | `docs/ops/research/2026-09-22-TOP_SHOPIFY_APPS.md` |
| **Real Problems** | Public complaints → solvable pains inside Overview→Orders→Customers→Spend→Goals | Docs only | `docs/ops/research/2026-09-22-REAL_OPERATOR_PAINS.md` |
| **Gap Fill Planner** | Merge the four above into PASS / HOLD / REFUSE cooks (fewer bigger PRs) | Docs only | `docs/plans/2026-09-22-niche-gap-fill.md` + patch `docs/ops/ADD_BACKLOG_RANKED.md` absorb/next lines |

Conductor owns dispatch + scoreboard. Craft App / Reviewer stay on app and site ships already in flight (starter LTV, site niche). These five do **not** edit `app/` or invent Partner metrics (no fake stars/installs — quote the page or say not claimed).

## Hard locks (every agent)

- Painted IA: Overview → Orders → Customers → Spend → Goals. Growth/LTV = Customers chips only.
- $39 flat · 7-day trial · Total ROAS = Shopify sales ÷ entered spend or **—**
- **Refuse as “wins”:** COGS/P&L hero · pixels / path credit / MTA / Meta ROAS · GMV pricing · sixth tab · Amazon/Recharge · session/visitor “fix Analytics” · invent reviews/stars/installs · country/tag LTV while PCD L2 in review
- Proof > claims. Cite public URLs. Stale tip SHAs are defects.
- Live stays PARKED. No Partner Submit. No outbound sends.

## How this uses “100 subagents”

Optional fan-out **inside** each reader (e.g. one Compete sub-reader per competitor, one Top-Apps sub-reader per app) is allowed **only** as read-only helpers that return notes to the named agent above. They do not each open PRs. The Gap Fill Planner is the single merge writer for the cook queue.

## Done when

- Five research docs + gap-fill plan exist on a PR from tip.
- ADD board next PASS lines match Gap Fill (not the stale P0 list).
- Conductor can dispatch Craft on the top PASS without inventing scope.
